<?xml version="1.0" encoding="UTF-8"?>

<mapper namespace="org.zerock.mappers.bookMapper">

 	<insert id="create" parameterType="BookVO">
		insert into book(
			book_id,book_name,book_o_price,book_m_price,book_kind,book_edition,book_publish,book_hash,book_safe_yn,book_sell_state,book_content,
			book_date,book_status,person_id
			
		)values(
			book_id_seq.nextval, #{bookName}, #{bookOPrice}, #{bookMPrice},#{bookKind}, #{bookEdition},#{bookPublish},
			#{bookHash}, #{bookSafeYn}, #{bookSellState}, #{bookContent}, 
			SYSDATE,#{bookStatus},#{personId}
		)		
	</insert>
	
	
</mapper>